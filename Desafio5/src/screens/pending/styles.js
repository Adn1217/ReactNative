import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
        fontSize: 20,
        fontWeight: 'bold'
    },
    listContainer: {
        fontWeight: 'bold',
        alignItems: 'center',
        justifyContent: 'center',
    },
    flatList: {
        // maxHeight: 450,
        marginTop: '5%',
        maxHeight: '64%'
        // marginBottom: 280
    }
});